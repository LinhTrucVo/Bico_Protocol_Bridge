@startuml serialToAnalogAppStaticStructure
skinparam backgroundColor #FEFEFE

package "serialToAnalogApp" {
    interface ISerialToAnalogApp {
        + Init() : Status_t
        + DeInit() : Status_t
        + ReadAdc(channel, pRawValue) : Status_t
    }

    class SerialToAnalogApp_Context_t {
        - initialized: bool
    }

    ISerialToAnalogApp --> SerialToAnalogApp_Context_t : manages
}

package "layer1Driver" {
    interface IAdcDriver {
        + AdcDriver_Read(channel, pValue) : Status_t
    }
}

ISerialToAnalogApp --> IAdcDriver : delegates

note right of ISerialToAnalogApp
  No UDS awareness.
  Called by CAC for RID 0x0101.
  Parameters: channel (uint8_t)
  Returns: rawValue (uint16_t)
end note
@enduml